{
  "name": "vectosolve-action",
  "version": "1.0.0",
  "description": "GitHub Action to convert raster images to SVG using VectoSolve AI",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build src/index.ts -o dist",
    "prepare": "npm run build"
  },
  "keywords": [
    "github-action",
    "svg",
    "vectorize",
    "image-conversion",
    "ai",
    "vectosolve"
  ],
  "author": "VectoSolve (https://vectosolve.com)",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/io": "^1.1.3"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0"
  }
}
